package org.example.view;

import org.example.observer.Observer;
import org.example.observer.event.Event;

public class ChessViewController implements GameView, Observer {
    @Override
    public void handle(Event e) {
        //Рисуй говно
    }
}
